---
phase: 02-content-scenes-navigation
plan: 05
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/sections/CaseFilesSection.tsx
  - src/components/ui/CaseFolder.tsx
  - src/components/ui/StampBadge.tsx
  - src/__tests__/casefiles.test.ts
autonomous: true
requirements: [CASE-01, CASE-02, CASE-03, CASE-04, CASE-05, CASE-06]

must_haves:
  truths:
    - "All 3 projects (Influence Maximization, NER Radar, UniWay) are rendered as case file folders"
    - "Each case file has a rubber stamp overlay showing status (Classified/Solved/Active)"
    - "Clicking a folder opens it with a flip animation revealing details"
    - "Expanded case file shows description, technologies, highlights, and links"
    - "Links to publications/demos are clickable"
  artifacts:
    - path: "src/components/sections/CaseFilesSection.tsx"
      provides: "Case files section with 3 project folders"
    - path: "src/components/ui/CaseFolder.tsx"
      provides: "Expandable case file folder with flip animation"
    - path: "src/components/ui/StampBadge.tsx"
      provides: "Reusable status stamp component"
  key_links:
    - from: "src/components/sections/CaseFilesSection.tsx"
      to: "src/data/projects.ts"
      via: "import caseFiles array"
      pattern: "import.*projects"
    - from: "src/components/ui/CaseFolder.tsx"
      to: "motion/react"
      via: "motion.div for folder flip + AnimatePresence for details"
      pattern: "rotateX|AnimatePresence"
---

<objective>
Build the Case Files section displaying all 3 projects as detective case file folders with flip-open animation, rubber stamp status overlays, and expandable details with links.

Purpose: Presents Akhila's projects as detective case files being investigated, maintaining the BSD agency metaphor.
Output: CaseFilesSection with 3 interactive folders, CaseFolder with flip animation, StampBadge reusable component.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-scenes-navigation/02-RESEARCH.md
@.planning/phases/02-content-scenes-navigation/02-01-SUMMARY.md

<interfaces>
From src/data/projects.ts:
```typescript
export const caseFiles: CaseFile[] = [
  { id: "case-001", title: "Influence Maximization...", status: "Solved", description: "...", technologies: [...], links: [...], highlights: [...] },
  { id: "case-002", title: "NER Radar...", status: "Active", description: "...", technologies: [...], highlights: [...] },
  { id: "case-003", title: "UniWay...", status: "Classified", description: "...", technologies: [...], highlights: [...] },
];
```

From src/data/types.ts:
```typescript
export type CaseStatus = 'Solved' | 'Active' | 'Classified';
export interface CaseFile { id: string; title: string; status: CaseStatus; description: string; technologies: string[]; links?: { label: string; url: string }[]; highlights: string[]; }
```

From src/styles/textures.css:
- .stamp-classified -- existing classified stamp styling
- .texture-paper, .texture-aged-edge -- paper and aged edge textures
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StampBadge and CaseFolder components</name>
  <files>src/components/ui/StampBadge.tsx, src/components/ui/CaseFolder.tsx</files>
  <action>
1. Create `src/components/ui/StampBadge.tsx`:
   - Props: `status: CaseStatus`, `className?: string`
   - Renders a stamp label with the status text (CLASSIFIED, SOLVED, ACTIVE)
   - Styling per status:
     - Classified: border-classified-red text-classified-red
     - Solved: border-teal text-teal
     - Active: border-gold text-gold
   - Common styling: `inline-block border-3 font-mono text-sm font-bold uppercase tracking-widest px-3 py-1 rotate-[-3deg] opacity-80 select-none`
   - `aria-label="Status: {status}"`
   - Follow the pattern from 02-RESEARCH.md "Stamp Badge Variants"

2. Create `src/components/ui/CaseFolder.tsx` -- a 'use client' component:
   - Props: `caseFile: CaseFile`
   - Uses `useState` for `isOpen` toggle
   - Card container: relative, bg-midnight/60, border parchment/15, rounded, overflow-hidden

   Folder cover (visible when closed):
   - `motion.div` with `animate={{ rotateX: isOpen ? -120 : 0 }}` with `transformOrigin: 'top'`
   - Transition: `type: 'spring', stiffness: 200, damping: 25`
   - Contains: Case file title (font-serif text-parchment), case ID (font-mono text-parchment/40 text-xs)
   - `<StampBadge>` positioned in top-right corner, slightly rotated
   - `.texture-paper` overlay for folder cover texture
   - Cursor pointer, onClick toggles isOpen

   Expanded details (visible when open):
   - `AnimatePresence` wrapping a `motion.div`
   - `initial={{ opacity: 0, height: 0 }}`, `animate={{ opacity: 1, height: 'auto' }}`, `exit={{ opacity: 0, height: 0 }}`
   - Content:
     a. Description paragraph (text-parchment/70 text-sm)
     b. Technologies as tags/chips (inline-flex, font-mono text-xs, bg-parchment/10, rounded-full px-2 py-0.5)
     c. Highlights as bulleted list
     d. Links: `<a>` elements with href, target="_blank", rel="noopener noreferrer", text-gold hover:underline
   - Padding p-4 to p-6

   Accessibility:
   - Folder cover is a `<button>` with `aria-expanded={isOpen}`, `aria-label="Case file: {title}"`
   - Enter/Space toggles
   - `focus-visible:ring-2 focus-visible:ring-gold/50`

   Reduced motion: if active, set `transition={{ duration: 0 }}` on all motion elements
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>StampBadge renders status with correct colors and rotation. CaseFolder flips open to reveal details with spring animation, shows stamp, handles keyboard interaction.</done>
</task>

<task type="auto">
  <name>Task 2: Build CaseFilesSection with scroll animations and tests</name>
  <files>src/components/sections/CaseFilesSection.tsx, src/__tests__/casefiles.test.ts</files>
  <action>
1. Create `src/components/sections/CaseFilesSection.tsx` -- a 'use client' component:

   Structure (inside `<Section id="case-files" depth={3}>`):
   - `<h2>` heading: "CASE FILES // Investigation Archive" in font-serif text-gold
   - Grid/list of CaseFolder components: `grid grid-cols-1 tablet:grid-cols-2 desktop:grid-cols-3 gap-6`
   - Map over `caseFiles` array, render `<CaseFolder>` for each

   Scroll animations (useGSAP):
   - Staggered reveal of folders: `gsap.from('.case-folder', { y: 30, opacity: 0, stagger: 0.2, duration: 0.6, ease: 'power2.out' })`
   - ScrollTrigger on section container
   - Reduced motion guard
   - Set initial CSS on folders: `opacity-0 translate-y-8` + class `case-folder`

2. Create `src/__tests__/casefiles.test.ts`:
   - Test: renders all 3 case files
   - Test: each case file shows title and stamp badge
   - Test: stamp badge shows correct status per case file
   - Test: case file folders have aria-expanded attribute
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/casefiles.test.ts --reporter=verbose</automated>
  </verify>
  <done>Case Files section renders 3 project folders with stamps, scroll-in animation, responsive grid layout</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `npx vitest run --reporter=verbose` -- all tests pass
- Dev server: 3 case file folders with stamps, clicking opens to reveal details
</verification>

<success_criteria>
- All 3 projects rendered as case file folders
- Each folder shows title, case ID, and status stamp (Classified/Solved/Active)
- Stamps have correct colors (red/teal/gold) and slight rotation
- Clicking a folder flips it open with spring animation
- Expanded view shows description, tech tags, highlights, and links
- Links open in new tab
- Keyboard accessible (tab, enter/space to toggle)
- Responsive grid (1/2/3 cols)
- Reduced motion disables flip animation
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-scenes-navigation/02-05-SUMMARY.md`
</output>
