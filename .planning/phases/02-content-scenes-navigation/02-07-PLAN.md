---
phase: 02-content-scenes-navigation
plan: 07
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05, 02-06]
files_modified:
  - src/app/page.tsx
  - src/app/layout.tsx
  - src/__tests__/sections.test.ts
  - src/__tests__/accessibility.test.ts
  - src/__tests__/seo.test.ts
autonomous: false
requirements: [SEO-04, SEO-05]

must_haves:
  truths:
    - "All 6 sections render on the page in correct order"
    - "Nav panel links scroll to each section correctly"
    - "All interactive elements are keyboard-navigable"
    - "All images/visual elements have alt text or aria-labels"
    - "Heading hierarchy is valid (h1 > h2 > h3, no skips)"
    - "Reduced motion disables all animations globally"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Full page with all 6 sections assembled"
    - path: "src/__tests__/accessibility.test.ts"
      provides: "Accessibility validation tests"
    - path: "src/__tests__/seo.test.ts"
      provides: "SEO heading hierarchy tests"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/sections/*"
      via: "imports all 6 section components"
      pattern: "import.*Section"
    - from: "src/components/nav/NavPanel.tsx"
      to: "src/app/page.tsx"
      via: "ScrollTrigger reads section IDs from DOM"
      pattern: "trigger.*#"
---

<objective>
Assemble all 6 sections into the main page in correct order, adjust layout padding for nav panel integration, run accessibility and SEO audits, fix any issues, and do a final visual verification checkpoint.

Purpose: Ensures the complete portfolio page works as an integrated whole -- navigation, scroll tracking, animations, and accessibility all functioning together.
Output: Complete page.tsx with all sections, passing accessibility and SEO tests, visual verification.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-scenes-navigation/02-RESEARCH.md
@.planning/phases/02-content-scenes-navigation/02-01-SUMMARY.md
@.planning/phases/02-content-scenes-navigation/02-02-SUMMARY.md
@.planning/phases/02-content-scenes-navigation/02-03-SUMMARY.md
@.planning/phases/02-content-scenes-navigation/02-04-SUMMARY.md
@.planning/phases/02-content-scenes-navigation/02-05-SUMMARY.md
@.planning/phases/02-content-scenes-navigation/02-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble page, adjust nav padding, create accessibility and SEO tests, fix issues</name>
  <files>src/app/page.tsx, src/app/layout.tsx, src/__tests__/sections.test.ts, src/__tests__/accessibility.test.ts, src/__tests__/seo.test.ts</files>
  <action>
1. Update `src/app/page.tsx`:
   - Remove demo/showcase content from Phase 1
   - Import all 6 section components: HeroSection, AboutSection, AbilitiesSection, CaseFilesSection, IntelSection, SocialSection
   - Render in order inside Shell:
     ```
     <Shell>
       <HeroSection />
       <AboutSection />
       <AbilitiesSection />
       <CaseFilesSection />
       <IntelSection />
       <SocialSection />
     </Shell>
     ```
   - Add left padding on desktop to prevent content overlap with the fixed NavPanel rail: `pl-20 tablet:pl-20` or similar on the main content wrapper. The NavPanel itself was already rendered in layout.tsx during Plan 01 -- do NOT re-add NavPanel here.

2. Update `src/app/layout.tsx` only if needed for padding adjustments:
   - NavPanel is already rendered here from Plan 01. Do NOT duplicate it.
   - If the padding for nav clearance is better applied at the layout level (wrapping {children} in a container with left padding on desktop), do it here instead of page.tsx. Choose whichever is cleaner -- the goal is that page content does not overlap the fixed nav rail.

3. Create `src/__tests__/sections.test.ts`:
   - Test: page renders all 6 section elements with correct IDs (hero, about, abilities, case-files, intel, social)
   - Test: sections appear in correct DOM order

4. Create `src/__tests__/seo.test.ts`:
   - Test: exactly one h1 element exists on the page
   - Test: h2 elements exist for each section
   - Test: no heading level skips (h1 -> h2, h2 -> h3, no h1 -> h3)

5. Create `src/__tests__/accessibility.test.ts`:
   - Test: all `<button>` elements have aria-label or visible text
   - Test: all `<a>` elements have aria-label or visible text
   - Test: all `<img>` elements have alt attribute
   - Test: nav links have aria-labels
   - Test: expandable elements have aria-expanded

6. Run the full test suite. Fix any failing tests by updating the relevant section components (e.g., missing aria-labels, wrong heading levels, missing alt text).

7. Run `npm run build` to verify no build errors with all sections assembled.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose && npm run build</automated>
  </verify>
  <done>All 6 sections rendered on page in correct order, NavPanel from Plan 01 still renders in layout.tsx (not duplicated), content has left padding to clear nav rail, all accessibility tests pass (aria-labels, alt text, keyboard nav), heading hierarchy valid, build succeeds</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 2 portfolio</name>
  <action>User visually verifies the complete portfolio with all 6 sections, navigation, scroll animations, accessibility, and responsive behavior.</action>
  <verify>User confirms all sections look correct and interactions work</verify>
  <done>User approves the visual and interactive state of the complete Phase 2 portfolio</done>
  <what-built>Complete Phase 2 portfolio with all 6 sections (Hero, About, Abilities, Case Files, Intel, Social), navigation panel, smooth scroll, and scroll-triggered animations</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. Verify nav panel is visible on left side with 6 labels
    3. Click each nav label -- should smooth-scroll to that section
    4. Verify active section highlights in gold in nav
    5. Scroll through the page and verify:
       a. Hero: parallax layers move at different speeds, typewriter title reveals, scroll indicator visible
       b. About: dossier fields animate in on scroll, education data (UTD, SRM) visible with GPAs
       c. Abilities: skill cards appear in category groups, hover/click shows description
       d. Case Files: 3 folders with stamps, clicking opens with flip animation
       e. Intel: 4 dossiers in timeline, redaction bars reveal on scroll, Autodesk has red accents
       f. Social: 3 links styled as agency panel
    6. Resize to mobile width (< 768px) -- nav should collapse to bottom bar
    7. Check keyboard navigation: Tab through interactive elements, Enter/Space to toggle cards
    8. Set system to reduced motion (or use DevTools) and refresh -- all animations should be disabled
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- all tests pass (including accessibility + SEO tests)
- `npm run build` succeeds
- Visual verification of complete portfolio
</verification>

<success_criteria>
- Complete portfolio page with all 6 sections in correct order
- Navigation works end-to-end (click to scroll, scroll to highlight)
- Content does not overlap with fixed NavPanel (proper left padding)
- All interactive elements keyboard navigable
- All images have alt text, all buttons have aria-labels
- Valid heading hierarchy (h1 -> h2 -> h3)
- Build succeeds with no errors
- User approves visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-scenes-navigation/02-07-SUMMARY.md`
</output>
