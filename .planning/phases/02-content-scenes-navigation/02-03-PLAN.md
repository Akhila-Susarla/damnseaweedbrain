---
phase: 02-content-scenes-navigation
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/sections/AboutSection.tsx
  - src/components/sections/SocialSection.tsx
  - src/__tests__/about.test.ts
autonomous: true
requirements: [ABOUT-01, ABOUT-02, ABOUT-03, ABOUT-04, SOCL-01, SOCL-02, SOCL-03, SOCL-04, SEO-01]

must_haves:
  truths:
    - "About section displays UTD and SRM education with GPAs"
    - "About section is styled as a BSD character dossier/personnel file"
    - "Leadership roles and achievements are integrated into the dossier"
    - "Dossier fields animate in on scroll"
    - "Social section renders LinkedIn, GitHub, and Email links"
    - "Social links are styled as an Agency communication panel"
    - "Proper heading hierarchy (h2 for section titles)"
  artifacts:
    - path: "src/components/sections/AboutSection.tsx"
      provides: "Character dossier with education, achievements, leadership"
    - path: "src/components/sections/SocialSection.tsx"
      provides: "Agency communication panel with social links"
  key_links:
    - from: "src/components/sections/AboutSection.tsx"
      to: "src/data/education.ts"
      via: "import educationEntries"
      pattern: "import.*education"
    - from: "src/components/sections/SocialSection.tsx"
      to: "src/data/social.ts"
      via: "import socialLinks"
      pattern: "import.*social"
---

<objective>
Build the About section as a BSD character dossier displaying education, achievements, and leadership roles with scroll-triggered reveals. Build the Social section as an Agency communication panel with BSD-styled links.

Purpose: About tells Akhila's story through the BSD personnel file lens. Social provides contact channels in a thematic wrapper.
Output: AboutSection and SocialSection components rendering real resume data with BSD styling and scroll animations.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-scenes-navigation/02-RESEARCH.md
@.planning/phases/02-content-scenes-navigation/02-01-SUMMARY.md

<interfaces>
From src/data/education.ts:
```typescript
export const educationEntries: Education[] = [
  { institution: "University of Texas at Dallas", degree: "MS Computer Science", gpa: "3.9", period: "2023 - 2025", highlights: [...] },
  { institution: "SRM Institute of Science and Technology", degree: "BS CS (AI/ML)", gpa: "9.51/10", period: "2019 - 2023", highlights: [...] },
];
```

From src/data/social.ts:
```typescript
export const socialLinks: SocialLink[] = [
  { platform: "LinkedIn", url: "...", label: "..." },
  { platform: "GitHub", url: "...", label: "..." },
  { platform: "Email", url: "mailto:...", label: "..." },
];
```

From src/data/types.ts:
```typescript
export interface Education { institution: string; degree: string; gpa?: string; period: string; highlights: string[]; }
export interface SocialLink { platform: string; url: string; label: string; }
```

From src/styles/textures.css:
- .texture-paper -- paper noise overlay
- .texture-aged-edge -- warm-toned aged edge shadows
- .stamp-classified -- classified stamp styling

From src/components/layout/Section.tsx:
```typescript
<Section id="about" depth={2}>...</Section>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build AboutSection as character dossier with scroll reveals</name>
  <files>src/components/sections/AboutSection.tsx, src/__tests__/about.test.ts</files>
  <action>
1. Create `src/components/sections/AboutSection.tsx` -- a 'use client' component:

   Structure (inside `<Section id="about" depth={2}>`):
   - `<h2>` section heading: "DOSSIER // Origin Story" in font-serif, text-gold
   - Character dossier card with `.texture-paper` and `.texture-aged-edge` CSS classes
   - Dossier is structured as a personnel file with labeled fields, NOT prose narrative

   Dossier fields (each as a `.dossier-field` div):
   a. **CODENAME:** "damnseaweedbrain" in font-mono
   b. **ORIGIN:** Map over `educationEntries` -- for each: institution name (font-serif, text-parchment), degree, GPA in parentheses (text-gold), period (text-parchment/60 font-mono text-xs)
   c. **KNOWN ABILITIES:** Pull highlights from education entries that represent achievements. Display as a bulleted list with dash-style bullets, font-mono text-sm
   d. **BACKGROUND:** Pull highlights related to leadership roles. Display similarly.

   The dossier card:
   - bg-midnight/80 with paper texture overlay
   - Border: 1px solid parchment/20
   - Padding generous (p-6 to p-10)
   - Max width ~800px, centered

   Scroll-triggered reveal (useGSAP):
   - Each `.dossier-field` animates in: `gsap.from('.dossier-field', { y: 30, opacity: 0, stagger: 0.15, duration: 0.8, ease: 'power2.out' })`
   - ScrollTrigger: trigger is the container, start 'top 80%', toggleActions 'play none none reverse'
   - Scope to containerRef
   - Guard with `if (reducedMotion) { gsap.set('.dossier-field', { opacity: 1, y: 0 }); return; }`

   Set initial CSS state on dossier-field elements: `opacity-0 translate-y-8` (matches GSAP from values to prevent hydration mismatch).

   Heading hierarchy: h2 for section, h3 for each field label.

2. Create `src/__tests__/about.test.ts`:
   - Test: renders both education entries (UTD and SRM)
   - Test: displays GPA values
   - Test: has h2 heading
   - Test: has dossier-field elements
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/about.test.ts --reporter=verbose</automated>
  </verify>
  <done>About section renders as a BSD character dossier with education entries, GPAs, achievements, and leadership roles. Scroll-triggered field reveals work. Proper heading hierarchy.</done>
</task>

<task type="auto">
  <name>Task 2: Build SocialSection as Agency communication panel</name>
  <files>src/components/sections/SocialSection.tsx</files>
  <action>
1. Create `src/components/sections/SocialSection.tsx` -- a 'use client' component:

   Structure (inside `<Section id="social" depth={4}>`):
   - `<h2>` heading: "COMMS // Agency Channel" in font-serif, text-gold
   - Panel card styled as a communication terminal:
     - bg-midnight/80, border parchment/20
     - Font-mono throughout for terminal aesthetic
     - Subtle scanline effect via CSS (optional, discretion)

   Social links:
   - Map over `socialLinks` array
   - Each link as an `<a>` element with:
     - `href={link.url}`, `target="_blank"`, `rel="noopener noreferrer"` (except email)
     - Platform name in uppercase tracking-widest
     - `aria-label="Contact via {platform}"`
     - Styled as a row: platform icon area (text character like "[LI]", "[GH]", "[@]" in monospace) + label text
     - Hover: text-gold transition, subtle underline
     - Focus-visible: outline ring for keyboard nav
   - Separator lines between links (border-b parchment/10)

   Scroll animation (useGSAP):
   - Staggered fade-in of each link: `gsap.from('.social-link', { x: -20, opacity: 0, stagger: 0.1, duration: 0.5 })`
   - ScrollTrigger on the section
   - Reduced motion guard
   - Set initial CSS: `opacity-0 -translate-x-5`

   Heading hierarchy: h2 for section title.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Social section renders LinkedIn, GitHub, Email links styled as an agency communication panel with monospace terminal aesthetic, proper aria-labels, keyboard-navigable links, scroll animation</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `npx vitest run --reporter=verbose` -- all tests pass
- Dev server: About section shows dossier with education data, Social shows themed links
</verification>

<success_criteria>
- About section displays UTD (MS CS, 3.9 GPA) and SRM (BS CS AI/ML, 9.51/10) in dossier format
- Dossier uses paper texture and aged edge styling from Phase 1
- Achievements and leadership roles visible in dossier fields
- Fields animate in on scroll with stagger
- Social section renders all 3 links in agency panel style
- All links have aria-labels and keyboard focus styles
- h2 headings for both sections
- Reduced motion disables all animations
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-scenes-navigation/02-03-SUMMARY.md`
</output>
