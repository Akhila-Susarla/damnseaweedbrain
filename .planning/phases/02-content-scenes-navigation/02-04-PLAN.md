---
phase: 02-content-scenes-navigation
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/sections/AbilitiesSection.tsx
  - src/components/ui/AbilityCard.tsx
  - src/__tests__/abilities.test.ts
autonomous: true
requirements: [ABIL-01, ABIL-02, ABIL-03, ABIL-04, ABIL-05]

must_haves:
  truths:
    - "All 32 resume skills are displayed as styled cards"
    - "Skills are organized into 4 category groups (Languages, DS/ML, Tools & Frameworks, Cloud)"
    - "Each card shows a tier badge (S/A/B/C) with distinct color per tier"
    - "Category headers include kanji subtitles"
    - "Cards animate in on scroll via GSAP ScrollTrigger"
    - "Hover/click on a card reveals the skill description"
  artifacts:
    - path: "src/components/sections/AbilitiesSection.tsx"
      provides: "Skills grid organized by category with animated reveals"
    - path: "src/components/ui/AbilityCard.tsx"
      provides: "Individual skill card with tier badge and detail interaction"
  key_links:
    - from: "src/components/sections/AbilitiesSection.tsx"
      to: "src/data/skills.ts"
      via: "import skills array, group by category"
      pattern: "import.*skills"
    - from: "src/components/ui/AbilityCard.tsx"
      to: "motion/react"
      via: "AnimatePresence for detail expand"
      pattern: "AnimatePresence"
---

<objective>
Build the Abilities section displaying all 32 resume skills as RPG ability cards organized by category with tier badges, animated scroll reveals, and hover/click detail interactions.

Purpose: Showcases Akhila's technical skills in the BSD ability/power system metaphor with visual hierarchy through tier ratings.
Output: AbilitiesSection with category-grouped skill cards, AbilityCard with tier badge and expandable description.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-scenes-navigation/02-RESEARCH.md
@.planning/phases/02-content-scenes-navigation/02-01-SUMMARY.md

<interfaces>
From src/data/skills.ts:
```typescript
export const skills: Skill[] = [
  { name: "Python", tier: "S", category: "Languages", kanjiName: "蛇語", description: "..." },
  // ... 32 total skills across 4 categories
];
```

From src/data/types.ts:
```typescript
export type SkillTier = 'S' | 'A' | 'B' | 'C';
export type SkillCategory = 'Languages' | 'Data Science/ML' | 'Tools & Frameworks' | 'Cloud';
export interface Skill { name: string; tier: SkillTier; category: SkillCategory; kanjiName?: string; description?: string; }
```

Theme tokens:
- gold (#d4a843) for S tier
- teal (#4a9e9e) for A tier
- parchment (#f5e6c8) for B tier
- classified-red for C tier (or use a muted variant)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AbilityCard component with tier badge and detail interaction</name>
  <files>src/components/ui/AbilityCard.tsx</files>
  <action>
1. Create `src/components/ui/AbilityCard.tsx` -- a 'use client' component:
   - Props: `skill: Skill`, `className?: string`
   - Card layout:
     - bg-midnight/60 border parchment/15, rounded, p-4
     - Skill name in font-sans text-parchment, text-sm font-medium
     - Large tier badge letter (S/A/B/C) -- text-2xl font-bold font-serif
     - Tier colors: S=text-gold, A=text-teal, B=text-parchment/80, C=text-parchment/50
     - Kanji subtitle below skill name if `kanjiName` exists -- text-xs text-parchment/40 font-mono
   - Hover/click detail interaction:
     - Use `useState` for `isExpanded`
     - On hover (desktop) or click/tap (mobile): toggle expanded state
     - Use `motion` `AnimatePresence` + `motion.div` for expand animation
     - When expanded: show `skill.description` below the card content in text-xs text-parchment/60
     - Transition: `initial={{ opacity: 0, height: 0 }}`, `animate={{ opacity: 1, height: 'auto' }}`
   - Accessibility:
     - Card is a `<button>` element (or has `role="button"` + `tabIndex={0}`)
     - `aria-expanded={isExpanded}`
     - `aria-label="{skill.name} - {skill.tier} tier ability"`
     - Enter/Space toggles expanded state
     - `focus-visible:ring-2 focus-visible:ring-gold/50`
   - Reduced motion: if active, set `transition={{ duration: 0 }}` on motion elements
   - Initial CSS for scroll animation compatibility: include `ability-card` class for GSAP targeting
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>AbilityCard renders skill name, tier badge with distinct color, kanji subtitle, expandable description on hover/click, keyboard accessible with aria-expanded</done>
</task>

<task type="auto">
  <name>Task 2: Build AbilitiesSection with category grid and scroll animations</name>
  <files>src/components/sections/AbilitiesSection.tsx, src/__tests__/abilities.test.ts</files>
  <action>
1. Create `src/components/sections/AbilitiesSection.tsx` -- a 'use client' component:

   Structure (inside `<Section id="abilities" depth={2}>`):
   - `<h2>` heading: "ABILITIES // Supernatural Registry" in font-serif text-gold
   - Group skills by category: `skills.reduce()` to create `Record<SkillCategory, Skill[]>`
   - For each category group:
     a. Category header: `<h3>` with category name in font-serif text-parchment + kanji subtitle from first skill's kanjiName area or hardcoded kanji per category (Languages: "言語", DS/ML: "科学", Tools: "道具", Cloud: "雲") in text-parchment/40 text-xs font-mono
     b. Grid of AbilityCard components: `grid grid-cols-2 tablet:grid-cols-3 desktop:grid-cols-4 gap-3`
   - Category sections separated with border-b parchment/10 and spacing

   Scroll animations (useGSAP):
   - Use `ScrollTrigger.batch('.ability-card', { ... })` for efficient batch animation
   - onEnter: `gsap.from(batch, { y: 20, opacity: 0, stagger: 0.05, duration: 0.4, ease: 'power2.out' })`
   - This animates cards in visible batches as user scrolls, more performant than individual triggers for 32 cards
   - Reduced motion guard: `gsap.set('.ability-card', { opacity: 1, y: 0 })`
   - Set initial CSS on cards: `opacity-0 translate-y-5`

2. Create `src/__tests__/abilities.test.ts`:
   - Test: renders all skills (check for skill names in the DOM)
   - Test: groups skills into 4 category sections (check for category headings)
   - Test: each card has correct tier badge
   - Mock `skills` data import for predictable testing
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/abilities.test.ts --reporter=verbose</automated>
  </verify>
  <done>Abilities section renders all 32 skills in 4 category groups with kanji headers, cards animate in on scroll via GSAP batch, grid is responsive (2/3/4 cols), reduced motion support</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `npx vitest run --reporter=verbose` -- all tests pass
- Dev server: abilities grid shows all skills by category with tier badges
</verification>

<success_criteria>
- All 32 skills rendered as styled cards
- 4 category groups with headers (Languages, DS/ML, Tools & Frameworks, Cloud)
- Kanji subtitles on category headers
- Tier badge (S/A/B/C) with gold/teal/parchment/muted colors
- Cards animate in on scroll with GSAP batch stagger
- Hover/click expands card to show description
- Cards are keyboard navigable with aria-expanded
- Responsive grid (2 cols mobile, 3 tablet, 4 desktop)
- Reduced motion shows all cards immediately
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-scenes-navigation/02-04-SUMMARY.md`
</output>
