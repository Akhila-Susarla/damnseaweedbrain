---
phase: 03-bsd-experience-layer
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/vn/HeroIntro.tsx
  - src/components/vn/SectionDialogue.tsx
  - src/components/sections/HeroSection.tsx
  - src/app/page.tsx
  - src/__tests__/hero-intro.test.ts
  - src/__tests__/dialogue-skip.test.ts
  - src/__tests__/section-dialogue.test.ts
autonomous: true
requirements:
  - HERO-04
  - VN-04
  - VN-05

must_haves:
  truths:
    - "On first load, Dazai introduces the visitor through a dialogue sequence in the hero section"
    - "Scroll is locked during hero intro until dialogue completes or ESC is pressed"
    - "ESC instantly skips the entire dialogue sequence and unlocks scroll"
    - "Click advances dialogue line-by-line (instant-complete if still typing, then advance)"
    - "Section transition dialogues appear when scrolling into new sections"
    - "Section transition dialogues do NOT lock scroll"
    - "All dialogues trigger once per session (fresh on page refresh, not stored in localStorage)"
    - "Portfolio content is never gated behind dialogue -- all sections remain accessible"
  artifacts:
    - path: "src/components/vn/HeroIntro.tsx"
      provides: "Hero intro dialogue with scroll lock"
      exports: ["default"]
    - path: "src/components/vn/SectionDialogue.tsx"
      provides: "Non-blocking section transition dialogue"
      exports: ["default"]
  key_links:
    - from: "src/components/vn/HeroIntro.tsx"
      to: "lenis"
      via: "useLenis stop()/start() for scroll lock"
      pattern: "lenis.*stop|lenis.*start"
    - from: "src/components/vn/HeroIntro.tsx"
      to: "src/components/vn/DialogueEngine.tsx"
      via: "renders DialogueEngine with hero-intro sequence"
      pattern: "DialogueEngine"
    - from: "src/components/vn/SectionDialogue.tsx"
      to: "gsap/ScrollTrigger"
      via: "onEnter callback triggers dialogue display"
      pattern: "ScrollTrigger.*onEnter"
    - from: "src/components/sections/HeroSection.tsx"
      to: "src/components/vn/HeroIntro.tsx"
      via: "HeroIntro rendered inside hero section"
      pattern: "HeroIntro"
---

<objective>
Wire the VN dialogue system into the page: hero intro with scroll lock, and section transition dialogues triggered by ScrollTrigger. Implement skip/advance controls and session-only tracking.

Purpose: Makes the VN system live -- visitors experience Dazai's narration on landing and as they scroll through sections.
Output: Working hero intro dialogue + 5 section transition dialogues, all skippable.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bsd-experience-layer/03-CONTEXT.md
@.planning/phases/03-bsd-experience-layer/03-RESEARCH.md
@.planning/phases/03-bsd-experience-layer/03-01-SUMMARY.md

@src/components/sections/HeroSection.tsx
@src/components/layout/SmoothScroll.tsx
@src/app/page.tsx
@src/lib/store.ts

<interfaces>
<!-- From Plan 01 outputs -->

From src/hooks/useDialogue.ts:
```typescript
export function useDialogue(sequence: DialogueSequence): {
  currentLine: DialogueLine | null;
  lineIndex: number;
  totalLines: number;
  advance: () => void;
  skip: () => void;
  isComplete: boolean;
  reset: () => void;
}
```

From src/components/vn/DialogueEngine.tsx:
```typescript
interface DialogueEngineProps {
  sequence: DialogueSequence;
  onComplete: () => void;
  className?: string;
}
export default function DialogueEngine(props: DialogueEngineProps): JSX.Element;
```

From src/data/dialogue/*.json:
```typescript
// All follow DialogueSequence schema
// hero-intro.json: type "intro", 3-5 lines
// *-transition.json: type "transition", 1-2 lines each
```

From src/lib/store.ts:
```typescript
dialogueActive: boolean;
setDialogueActive: (active: boolean) => void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HeroIntro component with scroll lock and skip behavior</name>
  <files>
    src/components/vn/HeroIntro.tsx,
    src/components/sections/HeroSection.tsx,
    src/__tests__/hero-intro.test.ts,
    src/__tests__/dialogue-skip.test.ts
  </files>
  <action>
    1. Create src/components/vn/HeroIntro.tsx:
       - 'use client' component
       - Import hero-intro.json dialogue data
       - On mount: lock scroll via useLenis().stop() and set dialogueActive=true in store
       - Render DialogueEngine with hero-intro sequence
       - Position: integrated within hero content area (not floating overlay per user decision). Place below the title, above the scroll indicator. Use absolute positioning within the hero's content overlay div (z-10 area).
       - On ESC keydown: skip dialogue, unlock scroll (lenis.start()), set dialogueActive=false
       - On DialogueEngine onComplete: unlock scroll, set dialogueActive=false, animate out
       - Track "hasPlayed" in local React state (not localStorage -- fresh per page refresh per user decision)
       - If hasPlayed, render nothing (return null)
       - Entry: motion/react fade-in with slight slide-up
       - Exit: fade-out, then remove from DOM
       - Keyboard: ESC handler added on mount, removed on unmount
       - Safety: cleanup function always calls lenis.start() in case component unmounts unexpectedly

    2. Modify src/components/sections/HeroSection.tsx:
       - Import HeroIntro (lazy is fine since it's in the same client boundary)
       - Add HeroIntro inside the hero content overlay div (the z-10 flex container), positioned between the subtitle and the scroll indicator
       - When dialogueActive is true (from store), hide the scroll indicator (opacity-0 or conditional render)
       - All existing parallax, typewriter, and scroll-down functionality remains unchanged

    3. Create test stubs:
       - src/__tests__/hero-intro.test.ts: test that HeroIntro renders dialogue, test that it calls onComplete
       - src/__tests__/dialogue-skip.test.ts: test that ESC key triggers skip, test that content is still accessible after skip
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/hero-intro.test.ts src/__tests__/dialogue-skip.test.ts -x</automated>
  </verify>
  <done>Hero section shows Dazai intro dialogue on load. Scroll is locked during intro. ESC skips instantly. Click advances lines. After completion, scroll unlocks and dialogue disappears. Scroll indicator hidden during dialogue.</done>
</task>

<task type="auto">
  <name>Task 2: SectionDialogue component with ScrollTrigger integration</name>
  <files>
    src/components/vn/SectionDialogue.tsx,
    src/app/page.tsx,
    src/__tests__/section-dialogue.test.ts
  </files>
  <action>
    1. Create src/components/vn/SectionDialogue.tsx:
       - 'use client' component
       - Props: { sectionId: string; dialogueData: DialogueSequence; className?: string }
       - Uses GSAP ScrollTrigger to detect when the target section enters viewport
       - ScrollTrigger config: trigger is the section element (by sectionId), start: "top 80%", once: true (fire only once per session)
       - When triggered: set local state "isVisible" to true, render DialogueEngine
       - Does NOT lock scroll (per user decision -- only hero intro locks)
       - Position: integrated at the top of the section content, before the section's own content
       - On DialogueEngine onComplete: animate out the dialogue, set isVisible to false
       - Click anywhere advances / ESC skips (handled by DialogueEngine internally)
       - Once played, does not replay (local state, resets on page refresh)
       - Small compact layout for 1-2 line quips: portrait smaller (w-16 h-24), bubble shorter

    2. Modify src/app/page.tsx:
       - Import SectionDialogue and all transition JSON files
       - Add SectionDialogue components between sections or as children at the top of each section:
         - Before/at AboutSection: about-transition.json, sectionId="about"
         - Before/at AbilitiesSection: abilities-transition.json, sectionId="abilities"
         - Before/at CaseFilesSection: casefiles-transition.json, sectionId="case-files"
         - Before/at IntelSection: intel-transition.json, sectionId="intel"
         - Before/at SocialSection: social-transition.json, sectionId="social"
       - Keep page.tsx as a server component if possible. If SectionDialogue needs client, create a small client wrapper component (e.g., SectionDialogues.tsx) that contains all 5 section dialogues, and import that into page.tsx.
       - Alternative: since page.tsx imports client components already (HeroSection etc.), adding SectionDialogue is fine -- they are client components rendered by a server parent.

    3. Create src/__tests__/section-dialogue.test.ts:
       - Test that SectionDialogue renders dialogue content when triggered
       - Test that it does not render before scroll trigger fires
       - Test that it disappears after completion
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/section-dialogue.test.ts -x && npm test</automated>
  </verify>
  <done>Scrolling into each section triggers a 1-2 line Dazai quip. Section dialogues do not lock scroll. Each plays only once per session. ESC or clicking through dismisses them. All sections remain fully accessible regardless of dialogue state.</done>
</task>

</tasks>

<verification>
- `npm test` -- all tests pass including new hero-intro, dialogue-skip, section-dialogue tests
- `npm run build` -- build succeeds
- Hero intro plays on page load, locks scroll, skippable with ESC
- Section transitions trigger on scroll, non-blocking
</verification>

<success_criteria>
- Hero intro dialogue plays with scroll lock on first load
- ESC instantly skips any dialogue and unlocks scroll
- Click advances dialogue line by line
- 5 section transition dialogues trigger on scroll entry
- Section dialogues never lock scroll
- All dialogues play once per session (fresh on refresh)
- Portfolio content never gated behind dialogue
</success_criteria>

<output>
After completion, create `.planning/phases/03-bsd-experience-layer/03-03-SUMMARY.md`
</output>
