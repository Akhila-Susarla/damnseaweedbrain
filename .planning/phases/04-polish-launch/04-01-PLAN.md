---
phase: 04-polish-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/fonts/PlayfairDisplay-Bold.ttf
  - assets/fonts/Inter-Regular.ttf
  - src/app/opengraph-image.tsx
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/__tests__/og-image.test.ts
  - src/__tests__/metadata.test.ts
  - src/__tests__/sitemap.test.ts
  - src/__tests__/robots.test.ts
autonomous: true
requirements:
  - ASSET-04
  - SEO-02

must_haves:
  truths:
    - "Sharing damnseaweedbrain.com on LinkedIn/Twitter shows a BSD-themed OG image with Akhila's name and Data Scientist title"
    - "Social crawlers receive correct Open Graph and Twitter Card meta tags in the HTML head"
    - "Google receives JSON-LD structured data identifying Akhila Susarla as a Person/Data Scientist with social links"
    - "Crawlers can discover all pages via /sitemap.xml and respect /robots.txt directives"
  artifacts:
    - path: "src/app/opengraph-image.tsx"
      provides: "BSD noir OG image generation (1200x630)"
      exports: ["default", "alt", "size", "contentType"]
    - path: "src/app/layout.tsx"
      provides: "Extended metadata with OG, Twitter, canonical, metadataBase"
      contains: "metadataBase"
    - path: "src/app/page.tsx"
      provides: "JSON-LD Person and WebSite structured data"
      contains: "application/ld+json"
    - path: "src/app/sitemap.ts"
      provides: "XML sitemap with damnseaweedbrain.com entry"
      exports: ["default"]
    - path: "src/app/robots.ts"
      provides: "Robots.txt rules allowing all crawlers"
      exports: ["default"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/opengraph-image.tsx"
      via: "Next.js file convention auto-populates openGraph.images from opengraph-image.tsx"
      pattern: "metadataBase.*damnseaweedbrain"
    - from: "src/app/page.tsx"
      to: "src/data/social.ts"
      via: "Social URLs used in Person JSON-LD sameAs field"
      pattern: "sameAs"
    - from: "src/app/robots.ts"
      to: "src/app/sitemap.ts"
      via: "Robots.txt references sitemap URL"
      pattern: "sitemap.*damnseaweedbrain"
---

<objective>
Create the BSD-themed OG image, complete SEO metadata, JSON-LD structured data, sitemap, and robots.txt for damnseaweedbrain.com.

Purpose: Fulfills the final two v1 requirements (ASSET-04, SEO-02) so the site renders correct social previews and is discoverable by search engines.
Output: OG image generator, extended metadata in layout.tsx, JSON-LD in page.tsx, sitemap.ts, robots.ts, and tests for all.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-launch/04-RESEARCH.md

@src/app/layout.tsx
@src/app/page.tsx
@src/data/social.ts
</context>

<interfaces>
<!-- Existing code the executor needs -->

From src/app/layout.tsx:
```typescript
export const metadata: Metadata = {
  title: "DamnSeaweedBrain",
  description: "A BSD-themed data science portfolio...",
};
// Fonts: Playfair_Display, Inter, JetBrains_Mono, Caveat (all via next/font/google)
```

From src/app/page.tsx:
```typescript
export default function Home() {
  return (
    <Shell>
      <HeroSection />
      {/* ... sections and overlays */}
    </Shell>
  );
}
```

From src/data/social.ts:
```typescript
export const socialLinks: SocialLink[] = [
  { platform: 'LinkedIn', url: 'https://linkedin.com/in/akhila-susarla-1803b41b6/', label: 'Akhila Susarla' },
  { platform: 'GitHub', url: 'https://github.com/akhilasusarla', label: 'akhilasusarla' },
  { platform: 'Email', url: 'mailto:akhilasusarla@gmail.com', label: 'akhilasusarla@gmail.com' },
];
```

Theme colors (from globals.css @theme):
- midnight: #0a0e1a
- gold: #c9a84c
- parchment: #e8e0d4
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OG image, sitemap, and robots.txt</name>
  <files>assets/fonts/PlayfairDisplay-Bold.ttf, assets/fonts/Inter-Regular.ttf, src/app/opengraph-image.tsx, src/app/sitemap.ts, src/app/robots.ts, src/__tests__/og-image.test.ts, src/__tests__/sitemap.test.ts, src/__tests__/robots.test.ts</files>
  <behavior>
    - OG image module exports: alt string containing "Akhila Susarla", size {width: 1200, height: 630}, contentType "image/png", and a default async function
    - sitemap() returns array with single entry: url "https://damnseaweedbrain.com", changeFrequency "monthly", priority 1
    - robots() returns rules allowing all user agents on "/" with sitemap URL "https://damnseaweedbrain.com/sitemap.xml"
  </behavior>
  <action>
    1. Download TTF fonts for Satori (WOFF2 not supported):
       ```bash
       mkdir -p assets/fonts
       curl -o assets/fonts/PlayfairDisplay-Bold.ttf "https://fonts.gstatic.com/s/playfairdisplay/v37/nuFvD-vYSZviVYUb_rj3ij__anPXJzDwcbmjWBN2PKdFvXDTbtPY_Q.ttf"
       curl -o assets/fonts/Inter-Regular.ttf "https://fonts.gstatic.com/s/inter/v18/UcCO3FwrK3iLTeHuS_nVMrMxCp50SjIw2boKoduKv0.ttf"
       ```

    2. Create `src/app/opengraph-image.tsx`:
       - Export `alt = 'Akhila Susarla - Data Scientist Portfolio'`
       - Export `size = { width: 1200, height: 630 }`
       - Export `contentType = 'image/png'`
       - Default export async function returning `new ImageResponse(...)` from `next/og`
       - BSD noir design: midnight background gradient (#0a0e1a to #111827), gold (#d4af37) accent lines, parchment (#e8e0d4) text
       - Content: "Akhila Susarla" in Playfair Display Bold 72px, tagline "Data Scientist | Armed Detective Agency" in Inter 32px gold
       - Load fonts via `readFile(join(process.cwd(), 'assets/fonts/...'))` -- NOT import
       - CRITICAL: Use ONLY flexbox layout, `display: 'flex'` on EVERY container div, inline styles only (Satori constraints)
       - No CSS Grid, no calc(), no CSS variables, no z-index

    3. Create `src/app/sitemap.ts`:
       - Export default function returning `MetadataRoute.Sitemap` array
       - Single entry: url "https://damnseaweedbrain.com", lastModified new Date(), changeFrequency "monthly", priority 1

    4. Create `src/app/robots.ts`:
       - Export default function returning `MetadataRoute.Robots`
       - Rules: userAgent "*", allow "/"
       - Sitemap: "https://damnseaweedbrain.com/sitemap.xml"
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/og-image.test.ts src/__tests__/sitemap.test.ts src/__tests__/robots.test.ts --reporter=verbose</automated>
  </verify>
  <done>OG image module exports correct metadata constants, sitemap returns damnseaweedbrain.com entry, robots allows crawling with sitemap reference. TTF fonts exist in assets/fonts/.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extended metadata and JSON-LD structured data</name>
  <files>src/app/layout.tsx, src/app/page.tsx, src/__tests__/metadata.test.ts, src/__tests__/jsonld.test.ts</files>
  <behavior>
    - layout.tsx metadata includes: metadataBase with damnseaweedbrain.com URL, title containing "Akhila Susarla" and "Data Scientist", description mentioning "Data Scientist", openGraph with type "website" and url, twitter with card "summary_large_image", alternates.canonical "/"
    - page.tsx renders two script tags with type "application/ld+json": one Person schema (name "Akhila Susarla", jobTitle "Data Scientist", sameAs array with LinkedIn and GitHub URLs) and one WebSite schema (name "DamnSeaweedBrain", url damnseaweedbrain.com)
  </behavior>
  <action>
    1. Update `src/app/layout.tsx` metadata export -- extend the existing object (do NOT create a second metadata export):
       - Add `metadataBase: new URL('https://damnseaweedbrain.com')`
       - Update `title: 'Akhila Susarla | Data Scientist'`
       - Update `description` to professional tone with subtle BSD flavor emphasizing "Data Scientist" keywords (Claude's discretion on exact copy)
       - Add `keywords: ['Data Scientist', 'Machine Learning', 'Portfolio', 'Akhila Susarla', 'Python', 'NLP']`
       - Add `authors: [{ name: 'Akhila Susarla' }]`
       - Add `openGraph` block: title, description (same as meta), url '/', siteName 'DamnSeaweedBrain', locale 'en_US', type 'website' -- images auto-populated by opengraph-image.tsx convention
       - Add `twitter` block: card 'summary_large_image', title, description -- images auto-populated
       - Add `alternates: { canonical: '/' }`
       - Do NOT add viewport/themeColor to metadata (those go in generateViewport in Next.js 14+)
       - Keep all existing font, layout, and component code unchanged

    2. Update `src/app/page.tsx` -- add JSON-LD script tags BEFORE the Shell component:
       - Import `socialLinks` from `@/data/social` to get LinkedIn and GitHub URLs for `sameAs`
       - Person schema: @context "https://schema.org", @type "Person", name "Akhila Susarla", jobTitle "Data Scientist", url "https://damnseaweedbrain.com", sameAs array (LinkedIn, GitHub URLs filtered from socialLinks -- exclude email)
       - WebSite schema: @context "https://schema.org", @type "WebSite", name "DamnSeaweedBrain", url "https://damnseaweedbrain.com"
       - Use `dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd).replace(/</g, '\\u003c') }}` pattern
       - Wrap JSON-LD scripts + existing Shell content in a fragment `<>...</>`
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/metadata.test.ts src/__tests__/jsonld.test.ts --reporter=verbose</automated>
  </verify>
  <done>layout.tsx metadata has metadataBase, OG tags, Twitter card, canonical URL all pointing to damnseaweedbrain.com. page.tsx renders Person and WebSite JSON-LD scripts with correct schema data. All tests pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest run --reporter=verbose` -- all existing + new tests pass
2. `npx next build` -- build succeeds without errors
3. Visit `/opengraph-image` in dev server to visually verify OG image renders with BSD noir aesthetic
4. Check built HTML for correct meta tags: `grep -i 'og:' .next/server/app/index.html` (or equivalent)
</verification>

<success_criteria>
- OG image generates a 1200x630 PNG with BSD noir design showing "Akhila Susarla" and "Data Scientist"
- HTML head contains Open Graph tags (og:title, og:description, og:image, og:type, og:url)
- HTML head contains Twitter Card tags (twitter:card = summary_large_image)
- JSON-LD Person and WebSite schemas render in page body
- /sitemap.xml returns valid sitemap with damnseaweedbrain.com
- /robots.txt allows all crawlers and references sitemap
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-launch/04-01-SUMMARY.md`
</output>
