---
phase: 01-foundation-theme-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - postcss.config.mjs
  - next.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/components/layout/Shell.tsx
  - src/components/layout/Section.tsx
  - src/lib/store.ts
  - src/lib/utils.ts
  - vitest.config.ts
  - src/__tests__/theme.test.ts
  - src/__tests__/store.test.ts
autonomous: true
requirements:
  - FNDN-01
  - FNDN-02
  - FNDN-03
  - FNDN-05
  - FNDN-06
  - ASSET-03

must_haves:
  truths:
    - "Next.js 15 dev server starts and renders a page with BSD midnight background and parchment text"
    - "Three font families (serif heading, sans body, monospace accent) render correctly via next/font"
    - "Tailwind v4 theme tokens generate working utility classes (bg-midnight, text-parchment, text-gold, font-heading, etc.)"
    - "Responsive shell adapts layout between 375px mobile and 1440px+ desktop"
    - "Zustand store initializes with correct default state shape"
    - "Vitest runs and all Wave 0 tests pass"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Tailwind v4 @theme tokens for all BSD colors, fonts, breakpoints"
      contains: "@theme"
    - path: "src/app/layout.tsx"
      provides: "Root layout with font variables, metadata, body classes"
      contains: "Playfair_Display"
    - path: "src/components/layout/Shell.tsx"
      provides: "Responsive layout container with max-width and responsive padding"
      min_lines: 10
    - path: "src/lib/store.ts"
      provides: "Zustand global store with section, dialogue, reducedMotion state"
      exports: ["usePortfolioStore"]
    - path: "vitest.config.ts"
      provides: "Test framework configuration"
      contains: "vitest"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import and font variable classes on html element"
      pattern: "className.*variable"
    - from: "src/app/globals.css"
      to: "Tailwind utilities"
      via: "@theme token definitions consumed by Tailwind v4"
      pattern: "@theme"
    - from: "src/app/layout.tsx"
      to: "src/components/layout/Shell.tsx"
      via: "Shell wraps page content"
      pattern: "Shell"
---

<objective>
Scaffold the Next.js 15 project with BSD noir theme system, responsive layout shell, Zustand global store, and Vitest test infrastructure.

Purpose: Establish the project foundation -- every component in Phase 2+ depends on these theme tokens, layout containers, and global state patterns being correct.
Output: A running Next.js 15 dev server rendering a BSD-themed page with all theme tokens, fonts, responsive shell, global store, and passing tests.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-theme-system/01-CONTEXT.md
@.planning/phases/01-foundation-theme-system/01-RESEARCH.md
@.planning/phases/01-foundation-theme-system/01-VALIDATION.md

No prior plans -- this is the first plan of the project.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project with BSD theme system, fonts, and responsive shell</name>
  <files>
    package.json, tsconfig.json, postcss.config.mjs, next.config.ts,
    src/app/layout.tsx, src/app/page.tsx, src/app/globals.css,
    src/components/layout/Shell.tsx, src/components/layout/Section.tsx,
    src/lib/utils.ts
  </files>
  <action>
    1. Scaffold project using `npx create-next-app@15 . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"` (use Next.js 15.x specifically, NOT 16).
       After scaffolding, verify package.json shows `"next": "15.x"`. If create-next-app installs v16, downgrade: `npm install next@15`.

    2. Verify `postcss.config.mjs` uses `"@tailwindcss/postcss": {}` (Tailwind v4 pattern). If it uses the old `tailwindcss` plugin, fix it. Verify `@tailwindcss/postcss` is installed. Do NOT create a tailwind.config.js -- Tailwind v4 uses CSS-first @theme.

    3. Replace `src/app/globals.css` with BSD theme tokens using Tailwind v4 @theme directive:
       ```css
       @import "tailwindcss";

       @theme {
         --color-midnight: #0a0e1a;
         --color-midnight-light: #111827;
         --color-midnight-deep: #060a14;
         --color-parchment: #e8e0d4;
         --color-parchment-dim: #c4b8a8;
         --color-gold: #d4af37;
         --color-gold-muted: #c9a84c;
         --color-teal: #2dd4bf;
         --color-teal-muted: #5eead4;
         --color-classified-red: #dc2626;

         --font-heading: var(--font-playfair-display);
         --font-body: var(--font-inter);
         --font-mono: var(--font-jetbrains-mono);

         --breakpoint-mobile: 375px;
         --breakpoint-tablet: 768px;
         --breakpoint-desktop: 1024px;
         --breakpoint-wide: 1440px;
       }
       ```
       Add base layer styles for body defaults (bg-midnight, text-parchment, font-body, antialiased).
       Add subtle gradient shift CSS custom properties for section depth variation (--section-depth-1 through --section-depth-4 using slightly different midnight values).

    4. Configure `src/app/layout.tsx` with three Google Fonts via next/font:
       - Playfair_Display (serif headings) with variable `--font-playfair-display`, subsets: ['latin'], display: 'swap'
       - Inter (sans body) with variable `--font-inter`, subsets: ['latin'], display: 'swap'
       - JetBrains_Mono (monospace accents) with variable `--font-jetbrains-mono`, subsets: ['latin'], display: 'swap'
       - Apply all three `.variable` classes to the `<html>` element
       - Set `<body className="bg-midnight text-parchment font-body antialiased">`
       - Add basic metadata: title "DamnSeaweedBrain", description about BSD-themed data science portfolio

    5. Create `src/components/layout/Shell.tsx`:
       - Responsive container: `min-h-screen bg-midnight`, main with `mx-auto max-w-[1440px] px-4 tablet:px-8 desktop:px-16`
       - Accept children prop with proper TypeScript typing

    6. Create `src/components/layout/Section.tsx`:
       - Reusable section wrapper accepting: id (string), className (optional), children
       - Applies full-width section with vertical padding: `py-16 tablet:py-24 desktop:py-32`
       - Supports optional depth variant prop for gradient shift backgrounds

    7. Create `src/lib/utils.ts` with a `cn()` utility combining clsx + twMerge pattern:
       `npm install clsx tailwind-merge`

    8. Update `src/app/page.tsx` to render a minimal demo page inside Shell with:
       - A heading in font-heading ("DamnSeaweedBrain") styled with text-gold
       - A body paragraph in font-body with text-parchment
       - A monospace accent line in font-mono with text-teal
       - One Section component demonstrating the layout
       This serves as a visual smoke test that theme tokens work.

    9. Verify `npm run dev` starts without errors and the page renders with correct colors and fonts.
  </action>
  <verify>
    <automated>cd "/mnt/c/Users/laksh/OneDrive/Documents/Codes/Untitled Portfolio" && npx next build</automated>
  </verify>
  <done>
    - Next.js 15 project builds successfully
    - Dev server renders page with midnight blue background, parchment text, gold heading, teal accent
    - Three font families load (serif, sans, mono) via next/font CSS variables
    - Tailwind v4 utility classes (bg-midnight, text-gold, font-heading, etc.) work
    - Responsive shell adjusts padding at tablet/desktop breakpoints
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Zustand store and Vitest test infrastructure with Wave 0 tests</name>
  <files>
    src/lib/store.ts,
    vitest.config.ts,
    src/__tests__/theme.test.ts,
    src/__tests__/store.test.ts
  </files>
  <action>
    1. Install test dependencies: `npm install --save-dev vitest @vitejs/plugin-react jsdom`

    2. Create `vitest.config.ts`:
       ```typescript
       import { defineConfig } from 'vitest/config';
       import react from '@vitejs/plugin-react';
       import path from 'path';

       export default defineConfig({
         plugins: [react()],
         test: {
           environment: 'jsdom',
           globals: true,
           setupFiles: [],
         },
         resolve: {
           alias: {
             '@': path.resolve(__dirname, './src'),
           },
         },
       });
       ```

    3. Create `src/lib/store.ts` with Zustand v5 pattern:
       - State shape: currentSection (string, default 'hero'), dialogueActive (boolean, default false), reducedMotion (boolean, default false), animationsReady (boolean, default false)
       - Actions: setCurrentSection, setDialogueActive, setReducedMotion, setAnimationsReady
       - Export the store hook as `usePortfolioStore`
       - Export the `PortfolioState` interface for testing

    4. Create `src/__tests__/store.test.ts`:
       - Test that store initializes with correct defaults (currentSection='hero', dialogueActive=false, reducedMotion=false)
       - Test that setCurrentSection updates currentSection
       - Test that setDialogueActive toggles dialogueActive
       - Test that setReducedMotion updates reducedMotion

    5. Create `src/__tests__/theme.test.ts`:
       - Read `src/app/globals.css` as a string (use fs.readFileSync)
       - Test that globals.css contains `@import "tailwindcss"`
       - Test that globals.css contains `@theme` block
       - Test that all required color tokens are defined: midnight, parchment, gold, teal, classified-red
       - Test that font tokens reference CSS variables: --font-heading, --font-body, --font-mono
       - Test that breakpoint tokens are defined: mobile (375px), tablet (768px), desktop (1024px), wide (1440px)

    6. Add `"test": "vitest run --reporter=verbose"` to package.json scripts.

    7. Run `npx vitest run --reporter=verbose` and confirm all tests pass.
  </action>
  <verify>
    <automated>cd "/mnt/c/Users/laksh/OneDrive/Documents/Codes/Untitled Portfolio" && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>
    - Zustand store exports usePortfolioStore with correct state shape and actions
    - Vitest configured and runs successfully
    - theme.test.ts passes: all color tokens, font tokens, breakpoint tokens verified in globals.css
    - store.test.ts passes: default values correct, all setters work
  </done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. `npx vitest run --reporter=verbose` -- all tests green
3. Dev server shows BSD-themed page with correct colors, fonts, responsive layout
</verification>

<success_criteria>
- Next.js 15 project scaffolded with App Router, TypeScript, Tailwind v4
- BSD noir theme tokens defined via @theme in globals.css (all colors, fonts, breakpoints)
- Three font families load via next/font with CSS variable integration
- Responsive Shell and Section layout components created
- Zustand v5 store with portfolio state shape
- Vitest running with Wave 0 tests for theme tokens and store
- `npx next build` succeeds, `npx vitest run` all green
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-theme-system/01-01-SUMMARY.md`
</output>
