---
phase: 04-polish-launch
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - next.config.ts
autonomous: false
requirements:
  - ASSET-04
  - SEO-02

must_haves:
  truths:
    - "Build output is under 1.5MB initial load with all assets optimized"
    - "Lighthouse Performance score is 90+ on the production build"
    - "Lighthouse SEO score is 90+ on the production build"
    - "Site is deployed and accessible at damnseaweedbrain.com"
    - "OG image renders correctly when URL is shared on social platforms"
  artifacts:
    - path: ".next/BUILD_OUTPUT"
      provides: "Successful production build"
    - path: "vercel.json"
      provides: "Vercel deployment config (if needed)"
  key_links:
    - from: "production build"
      to: "Vercel deployment"
      via: "vercel --prod or git push to linked repo"
      pattern: "vercel"
    - from: "damnseaweedbrain.com DNS"
      to: "Vercel project"
      via: "CNAME to cname.vercel-dns.com"
      pattern: "DNS"

user_setup:
  - service: vercel
    why: "Production hosting for Next.js portfolio"
    env_vars: []
    dashboard_config:
      - task: "Add custom domain damnseaweedbrain.com"
        location: "Vercel Dashboard -> Project Settings -> Domains"
      - task: "Configure DNS records at domain registrar"
        location: "Domain registrar -> DNS settings -> Add CNAME record: cname.vercel-dns.com (or A record: 76.76.21.21)"
---

<objective>
Verify performance budgets, run Lighthouse audit, fix any issues, and deploy the site to production on damnseaweedbrain.com.

Purpose: Ensures the completed portfolio meets quality standards and is publicly accessible for Akhila's job search.
Output: Validated production build, any performance fixes applied, site deployed to Vercel with custom domain.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-launch/04-RESEARCH.md
@.planning/phases/04-polish-launch/04-01-SUMMARY.md

@src/app/layout.tsx
@src/app/page.tsx
@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production build verification and performance audit</name>
  <files>next.config.ts, src/app/layout.tsx, src/app/page.tsx</files>
  <action>
    1. Run `npx next build` and capture output -- verify build succeeds and note bundle sizes.

    2. Verify total JS bundle is under 1.5MB initial load (baseline was 228kB -- should still be well under).

    3. Start production server: `npx next start` (background) and run Lighthouse CLI audit:
       ```bash
       npx lighthouse http://localhost:3000 --output=json --output-path=.planning/phases/04-polish-launch/lighthouse.json --chrome-flags="--headless --no-sandbox" --only-categories=performance,seo
       ```
       If Lighthouse CLI is not available or fails in this environment, document the scores that need manual verification.

    4. If any Lighthouse category scores below 90, apply fixes:
       - LCP issues: Check for render-blocking resources, optimize largest contentful paint element
       - CLS issues: Ensure all images/dynamic content have explicit dimensions
       - SEO issues: Verify meta tags are present in rendered HTML, check heading hierarchy
       - Apply fixes to the relevant files (layout.tsx, page.tsx, next.config.ts, or component files)

    5. If build and scores are satisfactory, verify the OG image renders by checking the build output:
       ```bash
       ls -la .next/server/app/opengraph-image*
       ```

    6. Run full test suite to confirm nothing is broken: `npx vitest run --reporter=verbose`

    No analytics scripts -- per user decision, skip third-party tracking entirely.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Production build succeeds. JS bundle under 1.5MB. OG image file generated in build output. All tests pass. Any Lighthouse issues below 90 are fixed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification, deployment, and social sharing test</name>
  <action>
    Human verifies the OG image renders correctly, deploys to Vercel, configures custom domain, and confirms social sharing previews work.

    Steps:
    1. Start dev server: `npm run dev`
    2. Visit http://localhost:3000/opengraph-image -- verify BSD noir OG image shows "Akhila Susarla" and "Data Scientist" tagline
    3. View page source (Ctrl+U) -- confirm:
       - og:title, og:description, og:image meta tags are present
       - twitter:card = summary_large_image is present
       - Two application/ld+json script blocks exist (Person + WebSite)
    4. Deploy to Vercel:
       - Run `npx vercel` to link and deploy (or push to GitHub if repo is linked)
       - Run `npx vercel --prod` for production deployment
       - Add domain damnseaweedbrain.com in Vercel Dashboard -> Settings -> Domains
       - Configure DNS at your registrar: CNAME record pointing to cname.vercel-dns.com
    5. After DNS propagates, verify https://damnseaweedbrain.com loads the portfolio
    6. Test social sharing: paste URL in LinkedIn post composer preview or use https://www.opengraph.xyz/ to verify OG tags
  </action>
  <verify>User confirms OG image looks good, site is live at damnseaweedbrain.com, and social previews render correctly</verify>
  <done>Site deployed to production. OG image approved. Social sharing metadata verified on LinkedIn/Twitter preview tools.</done>
</task>

</tasks>

<verification>
1. `npx next build` -- succeeds without errors
2. Bundle size under 1.5MB
3. OG image file exists in build output
4. All vitest tests pass
5. Site accessible at damnseaweedbrain.com (after user deploys)
6. Social preview shows correct OG image and metadata
</verification>

<success_criteria>
- Production build succeeds with JS bundle well under 1.5MB
- Lighthouse Performance >= 90 and SEO >= 90
- OG image renders BSD noir design with correct content
- Site deployed to damnseaweedbrain.com with SSL
- Social sharing preview shows correct image, title, and description
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-launch/04-02-SUMMARY.md`
</output>
