---
phase: 02-content-scenes-navigation
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/sections/HeroSection.tsx
  - src/components/ui/TypewriterText.tsx
  - src/__tests__/typewriter.test.ts
autonomous: true
requirements: [HERO-01, HERO-02, HERO-03, HERO-05, ANIM-04]

must_haves:
  truths:
    - "Hero section displays a Dazai character presence (image or styled placeholder)"
    - "Title text reveals with typewriter animation on page load"
    - "Three parallax layers move at different speeds on scroll"
    - "Scroll-down indicator is visible at bottom of hero and works"
  artifacts:
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Landing section with parallax, character, typewriter"
    - path: "src/components/ui/TypewriterText.tsx"
      provides: "Reusable typewriter text animation component"
  key_links:
    - from: "src/components/sections/HeroSection.tsx"
      to: "gsap/ScrollTrigger"
      via: "useGSAP parallax scrub animations"
      pattern: "scrub.*true"
    - from: "src/components/ui/TypewriterText.tsx"
      to: "src/lib/store.ts"
      via: "reducedMotion check to skip animation"
      pattern: "reducedMotion"
---

<objective>
Build the hero/landing section with 3-layer parallax, Dazai character presence, typewriter title reveal, and scroll-down indicator.

Purpose: The hero is the first thing visitors see -- it sets the BSD noir tone and draws them into the portfolio.
Output: HeroSection component with working parallax and typewriter animation, TypewriterText reusable component.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-scenes-navigation/02-RESEARCH.md
@.planning/phases/02-content-scenes-navigation/02-01-SUMMARY.md

<interfaces>
From src/components/layout/Section.tsx:
```typescript
// Section with id for scroll targeting, depth for gradient bg
<Section id="hero" depth={1}>...</Section>
```

From src/lib/store.ts:
```typescript
const reducedMotion = usePortfolioStore((s) => s.reducedMotion);
```

From globals.css theme tokens:
- --color-midnight (#0a0e1a), --color-parchment (#f5e6c8), --color-gold (#d4a843)
- --font-serif (Playfair Display), --font-mono (JetBrains Mono)
- Section depth 1: darkest background gradient
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TypewriterText component</name>
  <files>src/components/ui/TypewriterText.tsx, src/__tests__/typewriter.test.ts</files>
  <action>
1. Create `src/components/ui/TypewriterText.tsx` -- a 'use client' component:
   - Props: `text: string`, `className?: string`, `delay?: number` (seconds, default 0)
   - Uses `useGSAP` from `@gsap/react` for the animation
   - Renders a `<span>` with `aria-label={text}` for accessibility
   - Animation: GSAP timeline that reveals characters one by one (0.05s per char)
   - When `reducedMotion` is true from store: render full text immediately, no animation
   - Set initial textContent to empty string, progressively build it in onComplete callbacks
   - Follow the pattern from 02-RESEARCH.md "Typewriter Text Component"

2. Create `src/__tests__/typewriter.test.ts`:
   - Test: renders full text when reduced motion is active
   - Test: has aria-label with full text for screen readers
   - Test: renders empty initially when animation is active (mock GSAP)
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/typewriter.test.ts --reporter=verbose</automated>
  </verify>
  <done>TypewriterText component animates text character-by-character, respects reduced motion, has aria-label for accessibility</done>
</task>

<task type="auto">
  <name>Task 2: Build HeroSection with parallax and scroll indicator</name>
  <files>src/components/sections/HeroSection.tsx</files>
  <action>
1. Create `src/components/sections/HeroSection.tsx` -- a 'use client' component:

   Structure (inside `<Section id="hero" depth={1}>`):
   - Full viewport height (min-h-screen), relative positioning for parallax layers
   - Three parallax layers:
     a. Background layer (`.parallax-bg`): Dark Yokohama/noir atmosphere -- use CSS gradients and subtle textures from textures.css to create a moody cityscape silhouette effect. Radial gradient suggesting moonlight.
     b. Midground layer (`.parallax-mid`): Dazai character presence -- for now, create a styled CSS placeholder silhouette (tall figure shape using CSS clip-path or gradients) with a "Character art loading..." note. Use absolute positioning, centered. Later plans or user can swap in real character art. The component should use Next.js `<Image>` with explicit dimensions when real art is added.
     c. Foreground layer (`.parallax-fg`): Floating decorative elements -- scattered bandage strip shapes (rotated thin rectangles in parchment color), faint kanji characters (use CSS), subtle particle-like dots.

   Parallax animation (useGSAP):
   - Background: `y: -100` with `scrub: true`, trigger `#hero`, start 'top top', end 'bottom top'
   - Midground: `y: -50` with `scrub: true`, same trigger
   - Foreground: `y: 20` with `scrub: true`, same trigger
   - Guard all animations with `if (reducedMotion) return;`
   - Scope to heroRef

   Content overlay (above parallax layers, z-10):
   - `<TypewriterText>` with "damnseaweedbrain" in font-serif (Playfair Display), text-4xl to text-6xl responsive
   - Subtitle below: "Data Scientist | ML Engineer" in font-mono, text-parchment/70
   - Both centered vertically and horizontally

   Scroll-down indicator (bottom of hero):
   - Animated chevron/arrow pointing down
   - Text "Scroll to explore" in font-mono text-xs text-parchment/50
   - CSS animation: gentle bounce (translateY 0 to 8px), infinite
   - On click: use `useLenis` to scroll to '#about'
   - `aria-label="Scroll down to explore"`

   Heading hierarchy:
   - The typewriter title should be an `<h1>` element
   - Wrap Section with proper semantic structure

2. Ensure reduced motion guard disables all parallax and typewriter animation, showing static content.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Hero section renders full-viewport with 3-layer parallax on scroll, typewriter title animation, Dazai character placeholder, scroll-down indicator that scrolls to About section, h1 heading, reduced motion support</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `npx vitest run --reporter=verbose` -- all tests pass
- Dev server: hero fills viewport, parallax layers move on scroll, typewriter animates title
</verification>

<success_criteria>
- Hero section fills viewport with parallax depth effect
- Typewriter reveals "damnseaweedbrain" title character by character
- Character presence placeholder visible in midground layer
- Scroll indicator bounces at bottom, clicks to scroll to About
- Reduced motion shows static content with no animations
- h1 used for main title
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-scenes-navigation/02-02-SUMMARY.md`
</output>
