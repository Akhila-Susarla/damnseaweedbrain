---
phase: 03-bsd-experience-layer
plan: 05
type: execute
wave: 3
depends_on: ["03-03", "03-04"]
files_modified:
  - src/app/page.tsx
  - src/components/layout/Shell.tsx
autonomous: false
requirements:
  - HERO-04
  - VN-04
  - VN-05
  - ANIM-02
  - 3D-04
  - 3D-05

must_haves:
  truths:
    - "Full experience plays end-to-end: hero intro -> scroll through sections with transitions and dialogues -> 3D particles visible"
    - "ESC skip works at any point during dialogue"
    - "Reduced motion disables all animations, 3D, and transitions gracefully"
    - "No pointer events blocked by 3D canvas or ink wash overlays"
    - "Build succeeds and all tests pass"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Complete page assembly with all Phase 3 features"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/vn/SectionDialogue.tsx"
      via: "Section dialogues rendered between sections"
      pattern: "SectionDialogue"
    - from: "src/app/page.tsx"
      to: "src/components/transitions/InkWashTransition.tsx"
      via: "Ink wash overlays between sections"
      pattern: "InkWashTransition"
---

<objective>
Final assembly and visual verification of all Phase 3 features. Ensure VN dialogues, ink wash transitions, 3D particles, and tilt effects all work together without conflicts.

Purpose: Verifies the complete BSD experience layer works end-to-end before phase completion.
Output: Verified working experience with all Phase 3 features integrated.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bsd-experience-layer/03-CONTEXT.md
@.planning/phases/03-bsd-experience-layer/03-03-SUMMARY.md
@.planning/phases/03-bsd-experience-layer/03-04-SUMMARY.md

@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration check and conflict resolution</name>
  <files>
    src/app/page.tsx,
    src/components/layout/Shell.tsx
  </files>
  <action>
    1. Run full test suite and build to verify no conflicts between Plan 03 and Plan 04 changes:
       - Both plans modify page.tsx -- resolve any merge conflicts
       - Both plans touch HeroSection.tsx -- verify HeroIntro and Scene3D coexist (different z-indexes)
       - Verify Shell.tsx has FallbackProvider wrapping content

    2. Verify z-index layering in HeroSection:
       - z-0: parallax-bg (CSS background)
       - z-[1]: parallax-mid (Dazai silhouette)
       - z-[2]: parallax-fg (foreground decorations)
       - z-[3]: Scene3D (3D particles, pointerEvents:none)
       - z-10: content overlay (title, subtitle, HeroIntro dialogue, scroll indicator)
       - If z-indexes conflict, adjust so 3D is visible but below interactive content

    3. Verify ink wash transitions and section dialogues don't conflict:
       - InkWashTransition and SectionDialogue both trigger on section boundaries
       - InkWashTransition must have pointer-events:none so dialogue clicks work
       - SectionDialogue should appear above ink wash (higher z-index)

    4. Verify reduced motion path:
       - When prefers-reduced-motion is set: no particles, no ink wash, no tilt, dialogues show text immediately (no typewriter)
       - All content remains accessible

    5. Run: `npm test && npm run build`
       - Fix any issues found
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <done>All tests pass. Build succeeds. No z-index conflicts. No pointer event blocking. Reduced motion path works.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete BSD experience</name>
  <action>
    Human verifies the complete Phase 3 experience end-to-end. All automation is complete in Task 1; this checkpoint confirms the visual and interactive quality of the combined features.
  </action>
  <what-built>
    Complete BSD Experience Layer: visual novel dialogue system with hero intro (scroll-locked) and section transition dialogues, 3D floating particles in the hero section, mouse-follow tilt on ability cards and case file folders, and ink wash dissolve transitions between sections.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. **Hero Intro**: On page load, Dazai's dialogue should appear with typewriter text in handwritten-note styled bubbles. Character portrait with expression should be visible. Scroll should be locked.
    3. **Skip test**: Press ESC -- dialogue should instantly dismiss, scroll should unlock
    4. **Refresh and play through**: Refresh page, click through all 3-5 hero intro lines. After last line, dialogue disappears and scroll unlocks.
    5. **3D Particles**: In the hero section, floating particles (paper fragments, cherry blossoms, bandage strips) should drift downward with gentle sway. They should NOT block clicking on anything.
    6. **Section Dialogues**: Scroll down to the About section -- a 1-2 line Dazai quip should appear. Continue scrolling through each section, verifying dialogue appears at each transition.
    7. **Ink Wash Transitions**: While scrolling between sections, ink wash dissolve effect should play. Try scrolling backward -- the effect should reverse.
    8. **Card Tilt**: Hover over ability cards in the Abilities section -- they should tilt toward the cursor. Same for case file folders.
    9. **Nav Click**: Click a section in the nav panel -- it should use a quick fade (not ink wash) to transition.
    10. **Reduced Motion**: In browser DevTools, emulate prefers-reduced-motion:reduce. Refresh. Verify: no particles, no ink wash, no tilt, dialogues show text immediately without typewriter effect.
  </how-to-verify>
  <verify>Visual confirmation by human</verify>
  <done>Human approves the complete BSD experience layer</done>
  <resume-signal>Type "approved" if the experience works correctly, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm test` -- all tests pass
- `npm run build` -- build succeeds
- Visual verification of complete experience flow
</verification>

<success_criteria>
- All Phase 3 features work together without conflicts
- Hero intro plays with scroll lock, skippable with ESC
- Section dialogues trigger on scroll, non-blocking
- 3D particles visible in hero, auto-degrade on performance issues
- Ink wash transitions play between sections on scroll
- Card tilt works on ability cards and case folders
- Reduced motion disables all effects gracefully
- Human verification approves the complete experience
</success_criteria>

<output>
After completion, create `.planning/phases/03-bsd-experience-layer/03-05-SUMMARY.md`
</output>
