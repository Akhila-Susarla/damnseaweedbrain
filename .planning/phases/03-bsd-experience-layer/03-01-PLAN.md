---
phase: 03-bsd-experience-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/dialogue/hero-intro.json
  - src/data/dialogue/about-transition.json
  - src/data/dialogue/abilities-transition.json
  - src/data/dialogue/casefiles-transition.json
  - src/data/dialogue/intel-transition.json
  - src/data/dialogue/social-transition.json
  - src/data/types.ts
  - src/hooks/useDialogue.ts
  - src/components/vn/CharacterPortrait.tsx
  - src/components/vn/DialogueBox.tsx
  - src/components/vn/DialogueEngine.tsx
  - src/app/layout.tsx
  - src/__tests__/dialogue-engine.test.ts
  - src/__tests__/dialogue-data.test.ts
autonomous: true
requirements:
  - VN-01
  - VN-02
  - VN-03
  - VN-06

must_haves:
  truths:
    - "Dialogue engine advances through lines one at a time on trigger"
    - "Character portrait renders and swaps expressions based on current dialogue line"
    - "Speech bubble renders with handwritten-note styling (tilted, pen-stroke border, Caveat font)"
    - "All 6 JSON dialogue files parse correctly with expected schema"
  artifacts:
    - path: "src/hooks/useDialogue.ts"
      provides: "Dialogue state machine hook"
      exports: ["useDialogue"]
    - path: "src/components/vn/DialogueEngine.tsx"
      provides: "Core VN rendering component"
      exports: ["default"]
    - path: "src/components/vn/CharacterPortrait.tsx"
      provides: "Dazai portrait with expression variants"
      exports: ["default"]
    - path: "src/components/vn/DialogueBox.tsx"
      provides: "Handwritten-note speech bubble"
      exports: ["default"]
    - path: "src/data/dialogue/hero-intro.json"
      provides: "Hero intro dialogue sequence (3-5 lines)"
      contains: "lines"
  key_links:
    - from: "src/components/vn/DialogueEngine.tsx"
      to: "src/hooks/useDialogue.ts"
      via: "useDialogue hook consumption"
      pattern: "useDialogue"
    - from: "src/components/vn/DialogueEngine.tsx"
      to: "src/components/vn/CharacterPortrait.tsx"
      via: "expression prop from current line"
      pattern: "expression.*currentLine"
    - from: "src/components/vn/DialogueEngine.tsx"
      to: "src/components/vn/DialogueBox.tsx"
      via: "text prop from current line"
      pattern: "text.*currentLine"
---

<objective>
Build the core Visual Novel dialogue system: types, state machine hook, portrait component, speech bubble component, dialogue engine orchestrator, and all JSON dialogue data files. Add Caveat handwriting font.

Purpose: Establishes the complete VN component library that Plan 03 will wire into the hero intro and section transitions.
Output: Reusable VN components + all dialogue content in JSON + Caveat font integrated.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bsd-experience-layer/03-CONTEXT.md
@.planning/phases/03-bsd-experience-layer/03-RESEARCH.md

@src/components/ui/TypewriterText.tsx
@src/lib/store.ts
@src/data/types.ts
@src/app/layout.tsx

<interfaces>
<!-- Existing types and exports the executor needs -->

From src/data/types.ts (extend with dialogue types):
```typescript
export interface Skill { name: string; kanjiName?: string; tier: string; category: string; description?: string; }
export interface Education { ... }
export interface Experience { ... }
export interface Project { ... }
export interface SocialLink { ... }
```

From src/lib/store.ts:
```typescript
export interface PortfolioState {
  dialogueActive: boolean;
  setDialogueActive: (active: boolean) => void;
  // ... other fields
}
export const usePortfolioStore: ...
```

From src/components/ui/TypewriterText.tsx:
```typescript
interface TypewriterTextProps {
  text: string;
  className?: string;
  delay?: number;
  as?: React.ElementType;
}
export default function TypewriterText(props: TypewriterTextProps): JSX.Element;
```

From src/app/layout.tsx (fonts to extend):
```typescript
import { Playfair_Display, Inter, JetBrains_Mono } from "next/font/google";
// Add Caveat here
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dialogue types, JSON data files, useDialogue hook, and Caveat font</name>
  <files>
    src/data/types.ts,
    src/data/dialogue/hero-intro.json,
    src/data/dialogue/about-transition.json,
    src/data/dialogue/abilities-transition.json,
    src/data/dialogue/casefiles-transition.json,
    src/data/dialogue/intel-transition.json,
    src/data/dialogue/social-transition.json,
    src/hooks/useDialogue.ts,
    src/app/layout.tsx,
    src/__tests__/dialogue-engine.test.ts,
    src/__tests__/dialogue-data.test.ts
  </files>
  <behavior>
    - Test: useDialogue starts at line index 0 with isComplete=false
    - Test: advance() moves to next line, currentLine updates
    - Test: advance() on last line sets isComplete=true
    - Test: skip() immediately sets isComplete=true
    - Test: all 6 JSON files have valid DialogueSequence schema (id, section, type, lines array with id/character/expression/text)
    - Test: hero-intro.json has 3-5 lines with type "intro"
    - Test: transition JSONs have type "transition" and 1-2 lines each
  </behavior>
  <action>
    1. Add dialogue types to src/data/types.ts (append, do not modify existing types):
       - DialogueLine: { id: string; character: 'dazai'; expression: 'neutral' | 'smirk' | 'laugh' | 'serious' | 'annoyed' | 'mysterious'; text: string }
       - DialogueSequence: { id: string; section: string; type: 'intro' | 'transition'; lines: DialogueLine[] }

    2. Create all 6 JSON dialogue files in src/data/dialogue/:
       - hero-intro.json: 3-5 lines, type "intro", section "hero". Dazai starts mysterious then smirks. Content-aware: introduces visitor to the "Agency". Personality: cool/mysterious opening per user decision.
       - about-transition.json: 1-2 lines, type "transition". Quick quip about the character's "origin story". Expression: smirk or laugh.
       - abilities-transition.json: 1-2 lines, type "transition". References actual skills (e.g., Python joke). Expression: smirk.
       - casefiles-transition.json: 1-2 lines, type "transition". Quip about "cases" with humor. Expression: laugh or mysterious.
       - intel-transition.json: 1-2 lines, type "transition". Comment about "classified missions". Expression: serious.
       - social-transition.json: 1-2 lines, type "transition". Breaking fourth wall about "contacting the Agency". Expression: smirk or laugh.

    3. Create src/hooks/useDialogue.ts:
       - Takes DialogueSequence as param
       - State: lineIndex (number), isComplete (boolean)
       - Returns: { currentLine: DialogueLine | null, lineIndex, totalLines, advance: () => void, skip: () => void, isComplete, reset: () => void }
       - advance(): if not last line, increment lineIndex; if last line, set isComplete=true
       - skip(): set isComplete=true immediately
       - reset(): set lineIndex=0, isComplete=false
       - When isComplete, currentLine returns null

    4. Add Caveat font to src/app/layout.tsx:
       - Import Caveat from next/font/google
       - Configure with variable "--font-caveat", subsets: ["latin"], display: "swap"
       - Add variable to html className alongside existing font variables

    5. Create test files:
       - src/__tests__/dialogue-engine.test.ts: test useDialogue hook behavior (use renderHook from @testing-library/react)
       - src/__tests__/dialogue-data.test.ts: import all 6 JSON files, validate schema structure
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/dialogue-engine.test.ts src/__tests__/dialogue-data.test.ts -x</automated>
  </verify>
  <done>useDialogue hook passes all behavior tests. All 6 JSON dialogue files validate against schema. Caveat font variable added to layout.</done>
</task>

<task type="auto">
  <name>Task 2: CharacterPortrait, DialogueBox, and DialogueEngine components</name>
  <files>
    src/components/vn/CharacterPortrait.tsx,
    src/components/vn/DialogueBox.tsx,
    src/components/vn/DialogueEngine.tsx
  </files>
  <action>
    1. Create src/components/vn/CharacterPortrait.tsx:
       - 'use client' component
       - Props: { expression: DialogueLine['expression']; className?: string }
       - Renders an SVG-based Dazai portrait with swappable expression layers
       - Base: dark silhouette figure (similar to existing CSS clip-path Dazai in HeroSection but as SVG for flexibility)
       - Expression layers: SVG groups for eyes, mouth, eyebrows that swap based on expression prop
         - neutral: calm eyes, straight mouth, relaxed brows
         - smirk: one eye narrowed, slight upward curve on one side of mouth, one brow raised
         - laugh: closed/squinting eyes, wide smile, raised brows
         - serious: sharp eyes, straight/slight frown, level brows
         - annoyed: narrowed eyes, slight frown, one brow raised
         - mysterious: half-lidded eyes, subtle smile, level brows
       - Use motion/react AnimatePresence for smooth expression transitions (opacity fade, 200ms)
       - Respect useReducedMotion (skip transition animation if reduced motion)
       - Size: w-20 h-32 on mobile, w-28 h-40 on tablet+
       - Include bandage detail across one eye (Dazai's signature look)

    2. Create src/components/vn/DialogueBox.tsx:
       - 'use client' component
       - Props: { text: string; isTyping: boolean; onComplete?: () => void; className?: string }
       - Handwritten-note speech bubble per user decision:
         - Background: rgba(196, 184, 168, 0.08)
         - Border: 1.5px solid rgba(196, 184, 168, 0.25)
         - Border-radius: 4px 12px 12px 4px (asymmetric, like a torn note)
         - Transform: rotate(-0.5deg) (slightly tilted)
         - Font: font-caveat (the Caveat font added in Task 1) via Tailwind class
         - Box-shadow for pen-stroke effect: inset 0 0 0 1px rgba(196,184,168,0.05), 2px 2px 8px rgba(0,0,0,0.3)
         - Padding: 1rem 1.25rem
       - Text rendering: Use a dialogue-specific typewriter approach -- GSAP timeline that reveals characters one by one (similar to TypewriterText but with onComplete callback and ability to instantly complete on click)
       - When isTyping=false or reducedMotion=true, show full text immediately
       - On click anywhere on the box, if still typing, instantly show full text (call timeline.progress(1))
       - When typing completes, call onComplete callback
       - Add subtle "Dazai" name label above the bubble in monospace (font-mono text-xs text-parchment/40)

    3. Create src/components/vn/DialogueEngine.tsx:
       - 'use client' component
       - Props: { sequence: DialogueSequence; onComplete: () => void; className?: string }
       - Composes CharacterPortrait + DialogueBox + useDialogue hook
       - Layout: flex row -- portrait on left, dialogue box on right (stacks vertically on mobile)
       - On click anywhere in the engine area:
         - If DialogueBox is still typing the current line, instantly complete the typing
         - If typing is complete for current line, advance to next line
       - On ESC key: call skip() then onComplete()
       - When useDialogue.isComplete becomes true, call onComplete()
       - Entry animation: motion/react fade-in + slight slide-up (200ms)
       - Exit animation: fade-out (150ms)
       - Wrap in AnimatePresence for mount/unmount transitions
       - Pass currentLine.expression to CharacterPortrait
       - Pass currentLine.text to DialogueBox
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>CharacterPortrait renders SVG with 6 expression variants. DialogueBox renders handwritten-note styled bubble with typewriter text and click-to-complete. DialogueEngine orchestrates portrait + box with advance/skip/ESC behavior. All existing tests still pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/dialogue-engine.test.ts src/__tests__/dialogue-data.test.ts -x` -- all tests pass
- `npm test` -- full suite green (no regressions)
- `npm run build` -- no TypeScript errors, no SSR issues
</verification>

<success_criteria>
- useDialogue hook advances lines, skips, and resets correctly
- All 6 JSON files contain valid dialogue with content-aware Dazai quips
- CharacterPortrait renders with all 6 expression variants
- DialogueBox shows handwritten-note styling with typewriter text
- DialogueEngine composes portrait + box with click-advance and ESC-skip
- Caveat font loads and applies to dialogue bubbles
</success_criteria>

<output>
After completion, create `.planning/phases/03-bsd-experience-layer/03-01-SUMMARY.md`
</output>
